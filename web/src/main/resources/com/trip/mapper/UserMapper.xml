<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trip.web.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.trip.model.entity.User">
            <id property="id" column="id" />
            <result property="username" column="username" />
            <result property="password" column="password" />
            <result property="nickname" column="nickname" />
            <result property="avatarId" column="avatar_id" />
            <result property="phone" column="phone" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
            <result property="isDeleted" column="is_deleted" />
    </resultMap>

    <sql id="Base_Column_List">
        id,username,password,nickname,avatar_id,phone,
        create_time,update_time,is_deleted
    </sql>
    <select id="getAuthorVoByUserId" resultType="com.trip.model.vo.AuthorVO">
        SELECT 
            u.id as userId, 
            u.nickname, 
            u.username,
            COALESCE(gi.url, '') as avatar
        FROM user u
        LEFT JOIN graph_info gi ON u.id = gi.item_id AND gi.item_type = 1 AND gi.is_deleted = 0
        WHERE u.id = #{userId} AND u.is_deleted = 0
        LIMIT 1
    </select>

    <select id="getUserPostStatsByUserId" resultType="com.trip.model.vo.UserPostsStatsVO">
        select (select count(*)
                from post
                where post.user_id = #{userId} and post.is_deleted = 0) as postCount,

               (select count(*)
                from post_like
                where post_like.post_id in
                      (select post.id
                       from post
                       where post.user_id = #{userId} and post.is_deleted = 0)) as totalLikes
    </select>


    <resultMap id="UserPostMap" type="com.trip.model.vo.UserPostVO">
        <id property="postId" column="postId"/>

        <result property="tripName" column="tripName"/>
        <result property="createTime" column="create_time"/>

        <collection property="coverImages" ofType="java.lang.String">
            <result column="url"/>
        </collection>
    </resultMap>
    <select id="getUserPostsByUserId" resultMap="UserPostMap">
        select post.id as postId, trip.name as tripName, graph_info.url, post.create_time
        from post
        join trip on trip.id = post.trip_id and post.user_id = #{userId}
        left join graph_info on trip.id = graph_info.item_id and graph_info.item_type = 2
        where post.is_deleted = 0
    </select>


    <select id="getAuthorVoByKeyword" resultType="com.trip.model.vo.AuthorVO">
        SELECT 
            u.id as userId, 
            u.nickname, 
            u.username,
            COALESCE(gi.url, '') as avatar
        FROM user u
        LEFT JOIN graph_info gi ON u.id = gi.item_id AND gi.item_type = 1 AND gi.is_deleted = 0
        WHERE u.nickname LIKE CONCAT('%', #{keyword}, '%')
          AND u.is_deleted = 0
    </select>
</mapper>
